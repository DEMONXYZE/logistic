# Backend API (custom auth + REST, running via docker/make locally)
NEXT_PUBLIC_API_URL= http://localhost:8080

# Local mock backend (app/api/v1/*) — dev-only secret for signing JWTs
JWT_SECRET=dev-only-insecure-secret-change-me